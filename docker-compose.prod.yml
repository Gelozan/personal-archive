services:
  postgres:
    image: postgres:16-alpine
    container_name: pa_postgres
    env_file: ./infrastructure/env/postgres.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - pa_network
    restart: always

  backend:
    build: ./backend
    container_name: pa_backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    env_file: ./backend/.env
    networks:
      - pa_network
    restart: always
    depends_on:
      - postgres

  frontend:
    build: ./frontend
    container_name: pa_frontend
    networks:
      - pa_network
    restart: always
    depends_on:
      - backend

  nginx:
    image: nginx:1.27-alpine
    container_name: pa_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - backend
      - frontend
    networks:
      - pa_network
    restart: always

  prometheus:
    image: prom/prometheus:latest
    container_name: pa_prometheus
    volumes:
      - ./infrastructure/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=15d"
    networks:
      - pa_network
    restart: always

  grafana:
    image: grafana/grafana:latest
    container_name: pa_grafana
    env_file: ./infrastructure/env/monitoring.env
    volumes:
      - grafana_data:/var/lib/grafana
      - ./infrastructure/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    networks:
      - pa_network
    restart: always

  node-exporter:
    image: prom/node-exporter:latest
    container_name: pa_node_exporter
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"
    networks:
      - pa_network
    restart: always

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: pa_postgres_exporter
    env_file: ./infrastructure/env/postgres-exporter.env
    networks:
      - pa_network
    restart: always
    depends_on:
      - postgres

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:

networks:
  pa_network:
    driver: bridge
